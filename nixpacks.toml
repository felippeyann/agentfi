[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npm run db:generate --workspace=packages/backend"]

[start]
cmd = "node --import tsx/esm packages/backend/src/index.ts"
