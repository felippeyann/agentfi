name: Deploy Staging

# Railway auto-deploys on every push to develop.
# No manual steps needed — monitor at:
# https://railway.app/project/74e1a346-aeda-44a2-92b9-748c62e2872c

on:
  push:
    branches: [develop]

jobs:
  deploy-info:
    name: Railway Auto-Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Note
        run: echo "Railway deploys automatically on push to develop. Check the Railway dashboard for status."
