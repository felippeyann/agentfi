name: Deploy Production

# ⚠️  DISABLED — VPS-based deploy was replaced by Railway.
#
# When ready for a production release:
#   1. Set up a separate Railway environment (production)
#   2. Configure auto-deploy from the `master` branch (or tag-based)
#   3. Add PROD_DATABASE_URL, PROD_REDIS_URL and other secrets to Railway
#   4. Replace this workflow with a Railway CLI deploy or remove entirely
#      (Railway can handle it automatically without a workflow).
#
# Current state: all services run on Railway staging.
# Trigger: push a v*.*.* tag — currently a no-op.

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  pending-setup:
    name: Production Deploy (not yet configured)
    runs-on: ubuntu-latest
    steps:
      - name: Note
        run: |
          echo "Production deploy via Railway is not yet configured."
          echo "See the workflow file for instructions."
          exit 0
